import PlaylistPlayIcon from "@mui/icons-material/PlaylistPlay";
import * as React from "react";

const CurrentPlaylist = (props) => {
  return <PlaylistPlayIcon sx={{ color: "#fff", fontSize: "30px" }} />;
};

export default CurrentPlaylist;
